# Clerk — get from https://dashboard.clerk.com > API Keys
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...

# API base URL — your deployed ledg-api Vercel URL (no trailing slash)
# Leave unset in dev; the Vite proxy will forward /api calls to localhost:3000
VITE_API_URL=https://your-api.vercel.app/api/v1
